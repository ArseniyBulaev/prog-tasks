#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>

using std::cout, std::endl;





int main(){
    // int n;
    // std::cin >> n;
    // std::vector<size_t> ramanujam_numbers = generate_ramanujam_numbers_n_cube(n); 
    // std::for_each(ramanujam_numbers.begin(), ramanujam_numbers.end(), [](size_t ramanuj_number)
    // {std::cout << ramanuj_number << " ";});
    // std::cout << std::endl;
    // return 0;
}